# กติกาการแก้ไขโปรเจกต์นี้

## บังคับ — ขยับเลขเวอร์ชันทุกครั้งที่แก้โค้ด

`VERSION` ใน `claude_usage_widget.py` เป็นแหล่งเดียวของความจริง
`install.sh` อ่านค่านี้ไปใส่ `CFBundleVersion` และ `CFBundleShortVersionString`
ส่วน `make_dmg.sh` ใช้ตั้งชื่อไฟล์ `.dmg` — ถ้าไม่ขยับ ผู้ใช้จะแยกไม่ออกว่าตัวไหนใหม่กว่า

เกณฑ์ (semver):

| เปลี่ยนอะไร | ขยับตรงไหน | ตัวอย่าง |
|---|---|---|
| แก้บั๊ก, ปรับข้อความ, จัดรูปแบบโค้ด | patch → `1.4.1` | แก้ regex ที่ไม่ตรง |
| ฟีเจอร์ใหม่ หรือพฤติกรรมที่ผู้ใช้สังเกตเห็น | minor → `1.5.0` | เพิ่มโหมดแถบเมนู |
| เปลี่ยนจนของเดิมใช้ต่อไม่ได้ | major → `2.0.0` | ย้ายที่เก็บ config |

ขยับ **ในคอมมิตเดียวกับการแก้** ไม่แยกเป็นคอมมิตต่างหาก
ถ้าคอมมิตหนึ่งมีทั้งบั๊กฟิกซ์และฟีเจอร์ ให้ยึดระดับที่สูงกว่า

## ตรวจก่อน commit เสมอ

```bash
python3 -m py_compile claude_usage_widget.py
bash -n install.sh uninstall.sh make_dmg.sh notarize.sh
```

## ห้าม commit เด็ดขาด

- `sessionKey` หรือค่าใด ๆ ที่ดึงมาจาก Keychain
- `venv/`, `build/`, `dist/`, `setup.py` (สามอันแรกใหญ่มาก อันหลัง generate ใหม่ทุกครั้ง)
- ไฟล์ `.dmg`

## กับดักที่เคยเจอมาแล้ว

- **`.gitignore` ไม่รองรับ comment ท้ายบรรทัด** — `#` เป็น comment เฉพาะตอนอยู่ต้นบรรทัด
  เขียน `setup.py  # อธิบาย` = pattern กลายเป็นข้อความยาวทั้งบรรทัด ไม่ ignore อะไรเลย
- **`save_config()` เขียนทับทั้งไฟล์** — ต้อง `load_config()` มาแก้แล้วเขียนกลับเสมอ
  เคยมีบั๊กที่ลากย้าย widget แล้วค่าตั้งอื่นหายหมด เพราะส่ง `{"x":…, "y":…}` เข้าไปตรง ๆ
- **อย่าเรียก network บน main thread** — ใช้ `FetchWorker`/`QThread` แล้วส่งผลกลับด้วย signal
  ไม่งั้น UI ค้างตามค่า timeout
- **หน้าข่าว Anthropic ไม่มี RSS** — `NewsChecker` อ่าน HTML ตรง ๆ ถ้าเว็บเปลี่ยนโครงสร้าง
  ฟีเจอร์นี้ต้องเงียบไปเฉย ๆ ห้ามทำให้แอปพัง

## บริบทของโปรเจกต์

แอปเรียก endpoint ภายในของ claude.ai (`/api/organizations/{id}/usage`) ซึ่งไม่มีเอกสารรองรับ
โครงสร้าง response เปลี่ยนได้ตลอด โค้ดจึงต้องอ่านแบบเผื่อ key หายหรือเพิ่มเสมอ
